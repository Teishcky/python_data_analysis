import numpy as np

# t = np.arange(24).reshape(4, 6)

# print(np.argmax(t, axis=1))

np.random.seed(1)
t1 = np.random.randint(1, 20)
# np.random.seed(2)
t2 = np.random.randint(1, 20)

# t1 = np.random.uniform(19, 29, (3, 5))
print(t1, "  ", t2)
